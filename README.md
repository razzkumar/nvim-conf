# My nvim conf

## setup

  1. Install nvim
  2. Clone the repo
    ```
      git clone https://github.com/razzkumar/nvim-conf.git ~/.config/nvim
    ```
  3. Open nvim(install plugin manager automatically)
  4. ```:PlugInstall```
